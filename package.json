{
  "name": "gemini-vercel-app",
  "version": "1.0.0",
  "description": "Gemini API with Vercel Serverless Function",
  "type": "module",  
  "dependencies": {
    "@google/genai": "^0.15.0"
  },
  "scripts": {
    "start": "echo 'This project runs on Vercel, no local start needed for serverless function' && exit 1"
  }
}
